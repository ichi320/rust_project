use std::env;

fn chmax(a: u32, b: u32) -> u32 {
    if a > b {
        return a;
    }
    b
}

fn main() {
    for arg in env::args().skip(1) {
        a
    let a: u32 = 1;
    let b: u32 = 2;
    let c = chmax(a, b);
    println!("chmax is: {}", c);
}
